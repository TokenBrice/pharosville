# Claude Guide

Canonical repository instructions live in `AGENTS.md`.
Local dev server (maintained): http://localhost:5173/

1. Read `AGENTS.md`.
2. Read `docs/pharosville/AGENT_ONBOARDING.md`.
3. Run `npm run onboard:agent`.

Use `/agents/` for plans and `/outputs/` for scratch artifacts.
